NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Set as Provider secrets in Supabase Auth settings (not read directly by the app):
# Discord OAuth: client ID + secret from discord.com/developers/applications
# Google OAuth: client ID + secret from console.cloud.google.com

# Used server-side by the (future) moderation edge function:
OPENAI_API_KEY=
